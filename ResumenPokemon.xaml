<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokeAPI.ResumenPokemon"
             Title="Resumen Información Pokemon">
    <VerticalStackLayout HorizontalOptions="Center">
        <Image 
            x:Name="ImagePokemon"
            HeightRequest="300"/>
        <Label
            Text="Habilidades"
            FontAttributes="Bold"
            FontSize="Large"
            HorizontalOptions="Center"/>
        <Label
            x:Name="Habilidades"
            FontSize="Large"/>
    </VerticalStackLayout>
</ContentPage>
